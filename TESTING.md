# 迈科咖啡 · 真机测试检查清单

> 适用版本：`eb6826c` 之后｜后端 `https://api.mk-coffee.cn`｜真机测试前请先确认清单 0 前置条件

---

## 0. 前置条件（先确认）

- [ ] 小程序后台已配白名单：request / uploadFile / downloadFile 合法域名均含 `https://api.mk-coffee.cn`
- [ ] 开发者工具「详情 → 域名信息」里勾选「不校验合法域名」可临时跳过（真机预览必须配白名单，否则 request 全失败）
- [ ] 手机微信能正常打开 `https://api.mk-coffee.cn/api/`（浏览器访问返回 `{"code":0,...}`）
- [ ] 商品图可访问：手机浏览器打开 `https://api.mk-coffee.cn/static/products/yirgacheffe.jpg` 能显示图片

## 1. 登录（最关键，第一个测）

- [ ] 「我的」页点「微信登录」→ 弹出微信授权 → 提示「登录成功」
- [ ] 登录后头像昵称正确显示
- [ ] 杀掉小程序重新打开 → 登录态保持（不需要重新登录）
- [ ] 登录后「我的」页显示：订单/地址/优惠券/门店导航四个菜单

## 2. 首页

- [ ] 轮播图正常显示（2 张，可自动轮播、左右滑动）
- [ ] 分类横滚条可滑动，点击「袋装咖啡豆」过滤出对应商品
- [ ] 商品卡片显示图片（8 个商品都有图，不再只是 ☕）
- [ ] 商品卡片显示名称、分类、价格（如 ¥88.00）
- [ ] 点商品卡进入详情页

## 3. 商品详情

- [ ] 大图显示商品图（750×750 裁切无变形）
- [ ] 名称、分类、价格、描述正常
- [ ] 规格选择：点「200g / 500g」价格联动变化（¥88.00 ↔ ¥198.00）
- [ ] 数量 +/− 正常，合计金额同步（¥88.00 × 2 = ¥176.00）
- [ ] 未登录点「加入购物车」→ 弹窗引导去登录
- [ ] 登录后点「加入购物车」→ 提示成功

## 4. 购物车

- [ ] 购物车显示已加商品（带图、名称、规格、单价、小计）
- [ ] 勾选/取消勾选 → 底部合计金额变化
- [ ] 全选/取消全选
- [ ] 数量 +/−（修改后重新计算，接口 `update-qty` 正常）
- [ ] 左滑删除或删除按钮 → 确认弹窗 → 商品移除
- [ ] 同商品同规格再加一次 → 数量自动叠加（不重复出现两行）

## 5. 下单 + 支付（核心链路）

- [ ] 勾选商品 → 「确认下单」→ 弹窗显示合计金额
- [ ] 确认后跳转订单详情，提示「下单成功」
- [ ] 订单状态为「待支付」，金额正确
- [ ] 点「立即支付」→ 模拟支付直接提示「支付成功」
- [ ] 支付后订单状态变「已支付」（回列表刷新确认）
- [ ] 下单后购物车已清空（已下单商品消失）
- [ ] 库存校验：把某商品库存改成 1，下单 2 件 → 提示库存不足，不生成订单

## 6. 订单列表 / 详情

- [ ] 「订单」tab 显示订单列表（状态、金额、时间）
- [ ] 点订单进详情，明细商品、合计正确
- [ ] 待支付订单可取消 → 取消后状态「已取消」，库存恢复（可在商品详情确认库存+1）
- [ ] 已支付订单不能取消/重复支付（按钮置灰或不显示）

## 7. 优惠券

- [ ] 优惠券页「可领取」tab 显示可领券（如满减/折扣）
- [ ] 点「立即领取」→ 提示成功，「我的优惠券」出现该券
- [ ] 重复领取 → 提示已领取（不重复发放）
- [ ] 未登录访问优惠券页 → 引导登录

## 8. 收货地址

- [ ] 地址列表为空时显示空态
- [ ] 新增地址：姓名/手机号/地区/详细地址，手机号格式校验（11 位，`1[3-9]` 开头）
- [ ] 第一条地址自动设为默认
- [ ] 编辑地址、删除地址、设为默认均正常
- [ ] 未登录访问 → 弹窗引导登录

## 9. 门店导航

- [ ] 门店页显示地图 + 门店坐标 marker
- [ ] 点「导航」→ 调起微信打开位置/导航（使用门店真实坐标，不是用户位置）
- [ ] 拒绝定位权限 → 页面正常显示（不白屏），导航仍用门店坐标

## 10. 个人中心 / 边界

- [ ] 退出登录 → 回未登录态（首页购物车等未登录访问不报错）
- [ ] 退出后再登录，之前的数据（订单、地址）还在
- [ ] 弱网/断网 → 页面不白屏，有 toast 提示「网络异常」
- [ ] 快速连续点「加入购物车」→ 不重复加（或正常叠加，不报错）
- [ ] 桌面端（开发者工具切换 iPhone/Android/桌面）布局正常

---

## 数据核对（测试后回执）

测完在服务器核对数据，确认流程真实落库：

```bash
# 通过 dserver 跳板在腾服执行
ssh dserver 'ssh ubuntu@124.220.108.118 "cd ~/MK-Coffee && docker compose exec backend python manage.py shell -c \"
from users.models import User; from orders.models import Order
from payments.models import PaymentRecord
print(f\\\"用户: {User.objects.count()}, 订单: {Order.objects.count()}, 支付流水: {PaymentRecord.objects.count()}\\\")
\""'
```

预期：用户 ≥1、订单 = 你测试下的单数、支付流水 ≥ 订单数（模拟支付也会记流水）。

## 常见问题速查

| 现象 | 可能原因 |
|---|---|
| 真机所有 request 失败 | 白名单没配或配错域名（必须是 `.cn`） |
| 图片不显示 | 白名单 `downloadFile` 没配；或图片 404（看网络面板 URL） |
| 登录失败 | `WX_APP_SECRET` 不对（`app.js` 里配的是真实 appid，secret 在服务器 `.env`） |
| 支付按钮点了没反应 | 真实支付未启用（走 mock 应直接成功）；若走 `wechat_jsapi` 说明商户号已配但参数错 |
| 下单 500 | 看腾服日志 `docker compose logs backend --tail 50` |

---

## 提交审核前 Checklist

> 真机/体验版验收通过后、提交审核前逐项过一遍。

| # | 事项 | 说明 |
|---|---|---|
| 1 | **清理测试数据** | 体验期间产生的测试用户/订单/支付流水，提交审核前清干净（保留种子商品数据；可跑清库脚本） |
| 2 | **商品图换真实图** | 当前是占位图，能过审但影响印象分，建议至少换 4 张主推商品 |
| 3 | **门店信息确认** | `store.js` 里目前是苏州工业园区默认坐标，确认真实门店地址/坐标 |
| 4 | **隐私协议** | 微信要求配置《用户隐私保护指引》（后台 → 设置 → 服务内容声明），登录/地址功能会用到 |
| 5 | **服务类目** | 后台选择正确的类目（咖啡/食品销售类可能需要对应资质），类目不对直接拒审 |
| 6 | **备案号展示** | 小程序「关于」页展示 ICP 备案号（苏ICP备xxxxxx号） |
| 7 | **版本号** | 正式提交时版本号用 `1.0.0`，备注写清楚功能点 |

**建议节奏**：体验阶段放心测（测试数据提交审核前统一清理）→ 审核前 1~2 天逐项过 checklist。
